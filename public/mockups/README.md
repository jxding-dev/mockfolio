# Custom mockups

Place your transparent PNG mockup files in this folder, then register each asset in `manifest.json`.

```json
{
  "mockups": [
    {
      "id": "phone-dark",
      "label": "Dark phone",
      "src": "mockups/phone-dark.png"
    }
  ]
}
```
